package pages

import "testing"

// Scaffold smoke tests for routing; these are skipped until the test server is wired.
func TestSmoke_RootAndLogout(t *testing.T) {
	// TODO: Implement with testutil.NewTestServer and real HTTP calls to GET / and GET /logout
	t.Skip("scaffold test; implement with test server later")
}
