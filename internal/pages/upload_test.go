package pages

import "testing"

// Scaffold for Upload Step 1 tests (POST /upload/nbt)
func Test_Upload_Step1_Stub(t *testing.T) {
	// TODO: Once testutil.NewTestServer is implemented, send a multipart/form-data
	// request to POST /upload/nbt with a small .nbt fixture and assert validation
	// and preview response. For now we keep it skipped to maintain green tests.
	t.Skip("scaffold test; implement with test server and fixture later")
}
