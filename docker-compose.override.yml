# Local-only override: another project's dev postgres occupies host port 5433
# and its minio occupies 9000/9001. The app reaches both over the compose
# network; no host bindings needed.
services:
  postgres:
    ports: !override []
  minio:
    ports: !override []
