---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: createmod-com-dev-tls
  namespace: createmod-com-dev
spec:
  secretName: createmod-com-dev-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - createmod-com.9m.se
