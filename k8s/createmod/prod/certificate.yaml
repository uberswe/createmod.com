---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: createmod-com-prod-tls
  namespace: createmod-com-prod
spec:
  secretName: createmod-com-prod-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - createmod.com
    - www.createmod.com
