---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: createmod
  namespace: createmod-com-prod
  labels:
    app: createmod
    environment: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: createmod
  template:
    metadata:
      labels:
        app: createmod
        environment: prod
      annotations:
        linkerd.io/inject: enabled
        config.linkerd.io/skip-outbound-ports: "443,6379,9000"
    spec:
      terminationGracePeriodSeconds: 40
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: createmod
          image: ghcr.io/uberswe/createmod.com:master-e287358
          imagePullPolicy: Always
          ports:
            - containerPort: 8090
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: createmod-config
            - secretRef:
                name: createmod-secrets
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "5"]
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/ready
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 6
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 4Gi
